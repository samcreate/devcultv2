// Generated by CoffeeScript 1.7.0
(function() {
  var Roots, path, rimraf;

  path = require('path');

  Roots = require('../');

  rimraf = require('rimraf');

  module.exports = function(args, cli) {
    var output_path, project;
    project = new Roots(path.normalize(args._[1] || process.cwd()));
    output_path = project.config.output_path();
    rimraf.sync(output_path);
    return cli.emit('data', ("\nRemoved " + output_path + "\n").green);
  };

}).call(this);
